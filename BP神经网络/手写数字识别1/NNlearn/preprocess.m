function [ B ] = preprocess( A1 )
%图片预处理
%   


b = find( A1 >150);%I：读入的待辨认的数字图片，find(I>160)：找出I中大于160的坐标，返回的是线性索引
A1(b) = 255;%将图像中大于150的地方置1
b2 = find( A1 <=150);%I：读入的待辨认的数字图片，find(I>160)：找出I中大于160的坐标，返回的是线性索引
A1(b2) = 0;%将图像中大于150的地方置1
 % 对数字图片进行二值化处理，


B=A1;
end

